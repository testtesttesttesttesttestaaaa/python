name: Publish Pre-Release 
on:
  release:
   types: [published]
jobs:
  build:
   if: "!github.event.release.prerelease"
   runs-on: ubuntu-latest
   steps:
   - uses: actions/checkouti@master
   - run: |
        echo hello
